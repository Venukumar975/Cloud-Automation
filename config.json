{
  "apps": {
    "backend": {
      "source_1": {
        "directory": "./app/backend_api",
        "type": "node",
        "version": "18",
        "install-command": "npm install",
        "test-command": "npm test || echo \"no tests\"",
        "build-command": "echo \"no build needed\"",

        "app-env": ["PORT", "JWT_SECRET", "QR_JWT_SECRET"]
      }
    },

    "frontend": {
      "source_2": {
        "directory": "./app/teacher_app",
        "type": "node",
        "version": "18",
        "install-command": "npm install",
        "test-command": "npm test || echo \"no tests\"",
        "build-command": "npm run build"
      }
    }
  }
}
